<UserControl x:Class="Patient_Information_System_CS.Views.Admin.StaffsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000"
             Background="White">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header with Add Button -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Staff Account Management" 
                       FontSize="28" 
                       FontWeight="Bold"
                       Foreground="#2C3E50"
                       VerticalAlignment="Center"/>
            <Button Content="+ Add New Staff Account" 
                    Background="#28A745"
                    Foreground="White"
                    Padding="20,10"
                    FontSize="14"
                    FontWeight="SemiBold"
                    BorderThickness="0"
                    Cursor="Hand"
                    HorizontalAlignment="Right"
                    Click="AddStaffButton_Click"/>
        </Grid>

        <TabControl Grid.Row="1" FontSize="14">
            <TabItem Header="Active Staff" Padding="15,10">
                <Border BorderBrush="#DEE2E6" 
                        BorderThickness="1"
                        CornerRadius="5"
                        Padding="15">
                    <DataGrid x:Name="ActiveStaffGrid"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              CanUserDeleteRows="False"
                              IsReadOnly="True"
                              HeadersVisibility="Column"
                              GridLinesVisibility="Horizontal"
                              AlternatingRowBackground="#F8F9FA"
                              RowHeight="45">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Name"
                                                Width="*"
                                                Binding="{Binding DisplayName}"/>
                            <DataGridTextColumn Header="Staff ID"
                                                Width="1.2*"
                                                Binding="{Binding StaffProfile.StaffNumber}"/>
                            <DataGridTextColumn Header="Specialization"
                                                Width="1.5*"
                                                Binding="{Binding StaffProfile.Specialization}"/>
                            <DataGridTextColumn Header="License"
                                                Width="1.2*"
                                                Binding="{Binding StaffProfile.LicenseNumber, TargetNullValue=N/A, FallbackValue=N/A}"/>
                            <DataGridTextColumn Header="Role"
                                                Width="*"
                                                Binding="{Binding Role}"/>
                            <DataGridTextColumn Header="Email"
                                                Width="*"
                                                Binding="{Binding Email}"/>
                            <DataGridTextColumn Header="Contact"
                                                Width="*"
                                                Binding="{Binding PrimaryContactNumber}"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </TabItem>

            <TabItem Header="Pending Approval" Padding="15,10">
                <StackPanel>
                    <Border Background="#FFF3CD" 
                            BorderBrush="#FFC107"
                            BorderThickness="1"
                            Padding="15"
                            CornerRadius="5"
                            Margin="0,0,0,15">
                        <TextBlock x:Name="PendingStaffBanner"
                                   FontWeight="SemiBold"
                                   Foreground="#856404"/>
                    </Border>

                    <DataGrid x:Name="PendingStaffGrid"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              CanUserDeleteRows="False"
                              IsReadOnly="True"
                              HeadersVisibility="Column"
                              GridLinesVisibility="Horizontal"
                              AlternatingRowBackground="#F8F9FA"
                              RowHeight="45">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Name"
                                                Width="*"
                                                Binding="{Binding DisplayName}"/>
                            <DataGridTextColumn Header="Staff ID"
                                                Width="1.2*"
                                                Binding="{Binding StaffProfile.StaffNumber}"/>
                            <DataGridTextColumn Header="Specialization"
                                                Width="1.5*"
                                                Binding="{Binding StaffProfile.Specialization}"/>
                            <DataGridTextColumn Header="License"
                                                Width="1.2*"
                                                Binding="{Binding StaffProfile.LicenseNumber, TargetNullValue=N/A, FallbackValue=N/A}"/>
                            <DataGridTextColumn Header="Role"
                                                Width="*"
                                                Binding="{Binding Role}"/>
                            <DataGridTextColumn Header="Email"
                                                Width="*"
                                                Binding="{Binding Email}"/>
                            <DataGridTextColumn Header="Contact"
                                                Width="*"
                                                Binding="{Binding PrimaryContactNumber}"/>
                            <DataGridTemplateColumn Header="Action" Width="Auto">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Content="Approve"
                                                    Background="#28A745"
                                                    Foreground="White"
                                                    Padding="12,6"
                                                    BorderThickness="0"
                                                    Cursor="Hand"
                                                    Margin="0,0,8,0"
                                                    Click="ApproveStaff_Click"/>
                                            <Button Content="Reject"
                                                    Background="#DC3545"
                                                    Foreground="White"
                                                    Padding="12,6"
                                                    BorderThickness="0"
                                                    Cursor="Hand"
                                                    Click="RejectStaff_Click"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
