<UserControl x:Class="Patient_Information_System_CS.Views.Admin.AppointmentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000"
             Background="White">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Appointment Control Center"
                       FontSize="26"
                       FontWeight="Bold"
                       Foreground="#2C3E50"/>
            <TextBlock Text="Review pending requests and maintain the master schedule"
                       Foreground="#607D8B"/>
        </StackPanel>

        <TabControl Grid.Row="1" FontSize="14">
            <TabItem Header="Pending Requests" Padding="15,10">
                <StackPanel>
                    <Border Background="#FFF3CD"
                            BorderBrush="#FFC107"
                            BorderThickness="1"
                            Padding="15"
                            CornerRadius="5"
                            Margin="0,0,0,15">
                        <TextBlock x:Name="PendingAppointmentsBanner"
                                   FontWeight="SemiBold"
                                   Foreground="#856404"/>
                    </Border>

                    <DataGrid x:Name="PendingAppointmentsGrid"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              CanUserDeleteRows="False"
                              IsReadOnly="True"
                              HeadersVisibility="Column"
                              GridLinesVisibility="Horizontal"
                              AlternatingRowBackground="#F8F9FA"
                              RowHeight="45">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Date &amp; Time"
                                                Width="*"
                                                Binding="{Binding ScheduledFor, StringFormat={}{0:MMM dd, yyyy h:mm tt}}"/>
                            <DataGridTextColumn Header="Doctor"
                                                Width="*"
                                                Binding="{Binding DoctorName}"/>
                            <DataGridTextColumn Header="Patient"
                                                Width="*"
                                                Binding="{Binding PatientName}"/>
                            <DataGridTextColumn Header="Reason"
                                                Width="2*"
                                                Binding="{Binding Description}"/>
                            <DataGridTemplateColumn Header="Action" Width="Auto">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Content="Approve"
                                                    Background="#28A745"
                                                    Foreground="White"
                                                    Padding="12,6"
                                                    BorderThickness="0"
                                                    Cursor="Hand"
                                                    Margin="0,0,8,0"
                                                    Click="ApproveAppointment_Click"/>
                                            <Button Content="Reject"
                                                    Background="#DC3545"
                                                    Foreground="White"
                                                    Padding="12,6"
                                                    BorderThickness="0"
                                                    Cursor="Hand"
                                                    Click="RejectAppointment_Click"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </TabItem>

            <TabItem Header="Managed Schedule" Padding="15,10">
                <Border BorderBrush="#DEE2E6"
                        BorderThickness="1"
                        CornerRadius="5"
                        Padding="15">
                    <DataGrid x:Name="ManagedAppointmentsGrid"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              CanUserDeleteRows="False"
                              IsReadOnly="True"
                              HeadersVisibility="Column"
                              GridLinesVisibility="Horizontal"
                              AlternatingRowBackground="#F8F9FA"
                              RowHeight="45">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Date &amp; Time"
                                                Width="*"
                                                Binding="{Binding ScheduledFor, StringFormat={}{0:MMM dd, yyyy h:mm tt}}"/>
                            <DataGridTextColumn Header="Doctor"
                                                Width="*"
                                                Binding="{Binding DoctorName}"/>
                            <DataGridTextColumn Header="Patient"
                                                Width="*"
                                                Binding="{Binding PatientName}"/>
                            <DataGridTextColumn Header="Status"
                                                Width="*"
                                                Binding="{Binding StatusDisplay}"/>
                            <DataGridTextColumn Header="Notes"
                                                Width="2*"
                                                Binding="{Binding Description}"/>
                            <DataGridTemplateColumn Header="Action" Width="Auto">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Content="Mark Completed"
                                                    Background="#198754"
                                                    Foreground="White"
                                                    Padding="12,6"
                                                    BorderThickness="0"
                                                    Cursor="Hand"
                                                    Margin="0,0,8,0"
                                                    Click="CompleteAppointment_Click"/>
                                            <Button Content="Cancel"
                                                    Background="#6C757D"
                                                    Foreground="White"
                                                    Padding="12,6"
                                                    BorderThickness="0"
                                                    Cursor="Hand"
                                                    Click="CancelAppointment_Click"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
