<UserControl x:Class="Patient_Information_System_CS.Views.Admin.DoctorsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000"
             Background="White">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header with Add Button -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Doctor Account Management" 
                       FontSize="28" 
                       FontWeight="Bold"
                       Foreground="#2C3E50"
                       VerticalAlignment="Center"/>
            <Button Content="+ Register New Doctor" 
                    Background="#28A745"
                    Foreground="White"
                    Padding="20,10"
                    FontSize="14"
                    FontWeight="SemiBold"
                    BorderThickness="0"
                    Cursor="Hand"
                    HorizontalAlignment="Right"
                    Click="AddDoctorButton_Click"/>
        </Grid>

        <!-- TabControl -->
        <TabControl Grid.Row="1" FontSize="14">
            
            <!-- Active Doctors Tab -->
            <TabItem Header="Active Doctors" Padding="15,10">
                <Border Margin="15"
                        BorderBrush="#DEE2E6"
                        BorderThickness="1"
                        CornerRadius="5"
                        Padding="15">
                    <DataGrid x:Name="ActiveDoctorsGrid"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              CanUserDeleteRows="False"
                              IsReadOnly="True"
                              HeadersVisibility="Column"
                              GridLinesVisibility="Horizontal"
                              AlternatingRowBackground="#F8F9FA"
                              RowHeight="45">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Name" 
                                                Width="*"
                                                Binding="{Binding DisplayName}"/>
                            <DataGridTextColumn Header="Department" 
                                                Width="2*"
                                                Binding="{Binding DoctorProfile.Department}"/>
                            <DataGridTextColumn Header="Contact" 
                                                Width="*"
                                                Binding="{Binding DoctorProfile.ContactNumber}"/>
                            <DataGridTextColumn Header="Status" 
                                                Width="*"
                                                Binding="{Binding DoctorProfile.Status}"/>
                            <DataGridTemplateColumn Header="Actions" Width="Auto">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="Toggle Availability"
                                                Background="#0D6EFD"
                                                Foreground="White"
                                                Padding="12,6"
                                                BorderThickness="0"
                                                Cursor="Hand"
                                                Click="ToggleDoctorAvailability_Click"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </TabItem>

            <!-- Pending Approvals Tab -->
            <TabItem Header="Pending Approvals" Padding="15,10">
                <Grid Margin="15">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Info Banner -->
                    <Border Grid.Row="0" 
                            Background="#FFF3CD" 
                            BorderBrush="#FFC107"
                            BorderThickness="1"
                            Padding="15"
                            CornerRadius="5"
                            Margin="0,0,0,15">
                        <TextBlock x:Name="PendingDoctorsBanner"
                                   FontWeight="SemiBold"
                                   Foreground="#856404"/>
                    </Border>

                    <!-- DataGrid -->
                    <DataGrid x:Name="PendingDoctorsGrid"
                              Grid.Row="1" 
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              CanUserDeleteRows="False"
                              IsReadOnly="True"
                              HeadersVisibility="Column"
                              GridLinesVisibility="Horizontal"
                              AlternatingRowBackground="#F8F9FA"
                              RowHeight="50">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Name" 
                                                Width="*"
                                                Binding="{Binding DisplayName}"/>
                            <DataGridTextColumn Header="Department" 
                                                Width="2*"
                                                Binding="{Binding DoctorProfile.Department}"/>
                            <DataGridTextColumn Header="Contact" 
                                                Width="*"
                                                Binding="{Binding DoctorProfile.ContactNumber}"/>
                            <DataGridTextColumn Header="Application Date" 
                                                Width="*"
                                                Binding="{Binding DoctorProfile.ApplicationDate, StringFormat={}{0:MMM dd, yyyy}}"/>
                            <DataGridTemplateColumn Header="Actions" Width="Auto">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Content="Approve" 
                                                    Background="#28A745"
                                                    Foreground="White"
                                                    Padding="15,8"
                                                    BorderThickness="0"
                                                    Cursor="Hand"
                                                    Margin="5,0"
                                                    Click="ApproveDoctor_Click"/>
                                            <Button Content="Reject" 
                                                    Background="#DC3545"
                                                    Foreground="White"
                                                    Padding="15,8"
                                                    BorderThickness="0"
                                                    Cursor="Hand"
                                                    Margin="5,0"
                                                    Click="RejectDoctor_Click"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
            
        </TabControl>
    </Grid>
</UserControl>
