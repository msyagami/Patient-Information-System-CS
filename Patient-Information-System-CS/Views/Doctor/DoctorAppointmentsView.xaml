<UserControl x:Class="Patient_Information_System_CS.Views.Doctor.DoctorAppointmentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1200"
             Background="White">
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2.2*"/>
            <ColumnDefinition Width="24"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0"
                BorderBrush="#DEE2E6"
                BorderThickness="1"
                CornerRadius="6"
                Padding="18">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,12">
                    <TextBlock Text="My Schedule"
                               FontSize="24"
                               FontWeight="Bold"
                               Foreground="#2C3E50"/>
                    <Border x:Name="TodayBadge"
                            Background="#0D6EFD"
                            CornerRadius="12"
                            Margin="14,0,0,0"
                            Padding="10,4">
                        <TextBlock Text="Today"
                                   Foreground="White"
                                   FontWeight="SemiBold"/>
                    </Border>
                </StackPanel>

                <TextBlock Grid.Row="1"
                           Text="Select an appointment to review details or update its status"
                           Foreground="#607D8B"
                           Margin="0,0,0,12"/>

                <DataGrid Grid.Row="2"
                          x:Name="AppointmentsDataGrid"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          IsReadOnly="True"
                          HeadersVisibility="Column"
                          GridLinesVisibility="Horizontal"
                          AlternatingRowBackground="#F8F9FA"
                          RowHeight="42"
                          SelectionMode="Single"
                          SelectionChanged="AppointmentsDataGrid_SelectionChanged">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Start"
                                            Width="*"
                                            Binding="{Binding ScheduledFor, StringFormat={}{0:MMM dd, h:mm tt}}"/>
                        <DataGridTextColumn Header="Patient"
                                            Width="*"
                                            Binding="{Binding PatientName}"/>
                        <DataGridTextColumn Header="Status"
                                            Width="*"
                                            Binding="{Binding StatusDisplay}"/>
                        <DataGridTextColumn Header="Notes"
                                            Width="2*"
                                            Binding="{Binding Description}"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <Border Grid.Column="2"
                BorderBrush="#DEE2E6"
                BorderThickness="1"
                CornerRadius="6"
                Padding="22">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock x:Name="AppointmentTitleTextBlock"
                               Text="Select an appointment"
                               FontSize="24"
                               FontWeight="Bold"
                               Foreground="#2C3E50"
                               Margin="0,0,0,16"/>

                    <Grid Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="Patient" FontWeight="SemiBold"/>
                        <TextBlock Grid.Row="1" Text="Scheduled" FontWeight="SemiBold"/>
                        <TextBlock Grid.Row="2" Text="Status" FontWeight="SemiBold"/>
                        <TextBlock Grid.Row="3" Text="Concern" FontWeight="SemiBold"/>

                        <TextBlock x:Name="PatientNameTextBlock" Grid.Column="1" Text="-"/>
                        <TextBlock x:Name="ScheduledTextBlock" Grid.Column="1" Grid.Row="1" Text="-"/>
                        <TextBlock x:Name="StatusTextBlock" Grid.Column="1" Grid.Row="2" Text="-"/>
                        <TextBlock x:Name="DescriptionTextBlock" Grid.Column="1" Grid.Row="3" Text="-" TextWrapping="Wrap"/>
                    </Grid>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,18">
                        <Button x:Name="AcceptButton"
                                Content="Accept"
                                Background="#0D6EFD"
                                Foreground="White"
                                Padding="24,10"
                                FontWeight="SemiBold"
                                BorderThickness="0"
                                Margin="0,0,12,0"
                                Visibility="Collapsed"
                                Click="AcceptButton_Click"/>
                        <Button x:Name="CompleteButton"
                                Content="Mark Completed"
                                Background="#198754"
                                Foreground="White"
                                Padding="24,10"
                                FontWeight="SemiBold"
                                BorderThickness="0"
                                Margin="0,0,12,0"
                                Visibility="Collapsed"
                                Click="CompleteButton_Click"/>
                        <Button x:Name="CancelButton"
                                Content="Cancel"
                                Background="#6C757D"
                                Foreground="White"
                                Padding="20,10"
                                FontWeight="SemiBold"
                                BorderThickness="0"
                                Visibility="Collapsed"
                                Click="CancelButton_Click"/>
                    </StackPanel>

                    <Border Background="#E3F2FD"
                            BorderBrush="#90CAF9"
                            BorderThickness="1"
                            CornerRadius="6"
                            Padding="14"
                            Visibility="Collapsed"
                            x:Name="InstructionBorder">
                        <TextBlock Text="Pending visits need your confirmation. Completed visits update the patient's history automatically."
                                   Foreground="#0D47A1"
                                   TextWrapping="Wrap"/>
                    </Border>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
